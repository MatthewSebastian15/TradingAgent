// Uniform sidebar widths across the app. Every in-page sidebar (Research,
// Chatbot history, etc.) MUST use these so collapsed/expanded rails line up
// with the global nav rail (also w-12). Change here to change all sidebars.
export const SIDEBAR_COLLAPSED_WIDTH = 'w-12'; // 48px — matches the global nav rail
export const SIDEBAR_EXPANDED_WIDTH = 'w-60'; // 240px
